// Interface

// So we have something called Interface

// A single class can inherit multiple interfaces.

// Class implements interface1, interface2.

// All variables should be initialized in Interface.

// Method should not have body.

// Every Method should be overrided in its child class.

interface Owner{
    String ownerName="venkat";
    int age=21;
    int num=123;
    
}
interface ChaiPodiSupplier{
    String sipplierName="Rambabu";
}
class ChaiShop implements Owner,ChaiPodiSupplier {
    String branchName;
    int cupsSold;
    int pricePerCup;
    
}
class Main {
    public static void main(String[] args) {
        ChaiShop branch1=new ChaiShop();
        System.out.println(branch1.ownerName);
        System.out.println(branch1.sipplierName);
    }
}
