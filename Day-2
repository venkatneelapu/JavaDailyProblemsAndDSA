// in java we can play with the lists by using these inbuilt methods
// add()-to add an element in a list at last.
// remove()-to remove any element by using indexing.
// size()-we can find the size of an list.
// set(index,element)-we can replace element at any index by using set.
// get()-we can get any element from list to console.

import java.util.*;
class Main{
    public static void main(String[] args){
        List<String> shop=new ArrayList<>();
        shop.add("Milk");
        shop.add("oil");
        shop.add("ghee");
        System.out.println(shop);
        shop.set(1,"milk");
        System.out.println(shop);
        shop.remove(1);
        System.out.println(shop);
        System.out.println(shop.get(1));
        System.out.println(shop.size());
    }
}

// Output:
// [Milk, oil, ghee]
// [Milk, milk, ghee]
// [Milk, ghee]
// ghee
// 2
